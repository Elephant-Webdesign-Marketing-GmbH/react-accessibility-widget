/**
 * Speech read mode for text-to-speech functionality
 */
export enum SpeechReadMode {
  /** Automatic continuous reading without visual highlighting */
  AUTO = "auto",
  /** Reading with word/sentence highlighting in the DOM */
  HIGHLIGHT = "highlight",
}
