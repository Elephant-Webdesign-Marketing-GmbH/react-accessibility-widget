/**
 * Stacking order for widget overlay UI.
 * Uses the maximum safe z-index so the dialog stays above typical site chrome,
 * contact widgets, and chat overlays.
 */
export const A11Y_WIDGET_Z_INDEX = 2_147_483_647;
